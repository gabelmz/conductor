# Python
.venv/
__pycache__/
*.pyc

# Runtime data (db, configs, logs, keys, uploads) — regenerated locally, never commit
data/

# Node / Electron
node_modules/
desktop/parker.asar
desktop/build/

# Bundled LLM models (large binaries)
models/

# Optional read-only token for private-repo auto-update (never commit)
desktop/gh-token

# Packaging output
dist/
dist-v*-publish/

# Local AI-coding-tool caches/config — not part of the app
.claude/
.opencode/
.swarm/
.eb/

# Stray debug/scratch dumps
local-tree.md
